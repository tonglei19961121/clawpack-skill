{
  "name": "clawpack",
  "description": "Backup and restore OpenClaw configuration - pack, unpack, backup, restore",
  "version": "1.0.0",
  "tools": [
    {
      "name": "clawpack_pack",
      "description": "Pack OpenClaw configuration into a local zip file. Use this when user wants to backup their config without GitHub or transfer to another device via file.",
      "parameters": {
        "type": "object",
        "properties": {
          "outputFile": {
            "type": "string",
            "description": "Optional output filename (default: auto-generated like clawpack-backup-1234567890.zip)"
          }
        }
      }
    },
    {
      "name": "clawpack_unpack",
      "description": "Unpack OpenClaw configuration from a local zip file. Use this when user wants to restore config from a file they received.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "Path to the pack file (.zip), e.g., 'clawpack-backup.zip' or '/path/to/backup.zip'"
          }
        },
        "required": ["file"]
      }
    },
    {
      "name": "clawpack_backup",
      "description": "Backup OpenClaw configuration to GitHub Gist or repository. Requires GitHub authentication. Good for cloud sync across devices.",
      "parameters": {
        "type": "object",
        "properties": {
          "full": {
            "type": "boolean",
            "description": "Include complete configuration (not just skills) - recommended"
          },
          "workspace": {
            "type": "boolean",
            "description": "Include workspace files (SOUL.md, AGENTS.md, etc.)"
          },
          "repo": {
            "type": "string",
            "description": "Push to a GitHub repository instead of Gist (format: owner/repo)"
          }
        }
      }
    },
    {
      "name": "clawpack_restore",
      "description": "Restore OpenClaw configuration from GitHub Gist or repository.",
      "parameters": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "Gist ID or repo (owner/repo). Optional if user has a default backup set."
          },
          "full": {
            "type": "boolean",
            "description": "Restore full configuration (not just skills) - recommended"
          }
        }
      }
    },
    {
      "name": "clawpack_status",
      "description": "Check clawpack installation status and GitHub authentication. Use this to verify setup.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "clawpack_list",
      "description": "List all installed OpenClaw skills. Shows what would be backed up.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
